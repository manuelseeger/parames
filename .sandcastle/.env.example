# Generate on a trusted host with `claude setup-token`.
# Never commit the populated .sandcastle/.env file.
CLAUDE_CODE_OAUTH_TOKEN=

# Read-only token for issue and repository metadata inside agent sandboxes.
GH_TOKEN=

# Start with one agent; increase only after observing subscription limits.
SANDCASTLE_CONCURRENCY=1
